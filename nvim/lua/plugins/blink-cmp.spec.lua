local utils = require("utils")

return {
  utils.getPlugin("blink.cmp", "saghen/blink.cmp", {
    -- opts = {
    --   keymap = { preset = "default" },
    --   sources = {
    --     "lsp", "buffer", "snippets", "path"
    --     -- "copilot", "dictionary", "emoji", "git",
    --     -- "nerdfont", "spell"
    --   }
    -- }
  }),
}

